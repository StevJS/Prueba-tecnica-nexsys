CREATE TABLE producto_nexsys (
    p_id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    description TEXT,
    buy_price BIGINT NOT NULL CHECK (buy_price >= 0),
    category_platzi BIGINT NOT NULL CHECK (category_platzi >= 0),
    image VARCHAR(255)
);